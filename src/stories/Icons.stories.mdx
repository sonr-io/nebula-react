<!-- Icon.stories.mdx -->

import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { NebulaIcon } from './../components/Icons';

---
name: Sonr Icon
route: /Components/**NebulaIcon**
---

<Meta
  title="Icons"
  component={NebulaIcon}
/>

# Icon
Sonr SSO NebulaIcon Icon component.

# Properties
```js
{
    dataTestid?: string;                          // custom test it to use with jest or other testing library
    iconName?: IconNames;                         // icon to be displayed
    iconType?: 'filled' | 'outline' | 'duotone';  // to set the icon variation for the name you provided (default 'filled')
}
```

export const Template = (args) => <NebulaIcon {...args} />

<Canvas>
  <Story name="Filled" args={{ iconName: 'Aave', iconType: 'filled', className: "w-6 h-6" }}>
    {Template.bind({})}
  </Story>
  <Story name="Outline" args={{ iconName: 'Aave', iconType: 'outline', className: "w-6 h-6" }}>
    {Template.bind({})}
  </Story>
  <Story name="DuoTone" args={{ iconName: 'Aave', iconType: 'duotone', className: "w-6 h-6" }}>
    {Template.bind({})}
  </Story>
</Canvas>
