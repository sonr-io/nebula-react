
<!-- RegisterForm.stories.mdx -->
import { useState } from 'react';
import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { RegisterForm } from './../components/RegisterForm';

---
name: Sonr SSO Login Button
route: /Components/sonr-register
---

<Meta
    title="RegisterForm for SSO"
    argTypes={{
        label: {
            name: "label rendered within control",
            description: "label to be rendered within the scope of the control as a child",
            control: {
                type: 'text'
            }
        }
    }}
    component={RegisterForm}
/>


# RegisterForm for SSO
Sonr SSO button component for registration on the sonr network.

<ArgsTable of={RegisterForm} />

export const Template = (args) => <RegisterForm {...args} />

<Canvas>
    <Story name="Form"
      argTypes={{
        onRegister: {
          control: false
        },
        onError: {
          control: false
        }
      }}
      args={{
        domain: 'domain.snr',
        onRegister: result => alert(`Registered in! ${result}`),
        onError: err => alert(`Error: ${err}`)
    }}>
        {Template.bind({})}
    </Story>
</Canvas>
