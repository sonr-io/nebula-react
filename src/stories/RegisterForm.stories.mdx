
<!-- RegisterForm.stories.mdx -->
import { useState } from 'react';
import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { RegisterForm } from './../components/RegisterForm';

---
name: Sonr SSO Register Form
route: /Components/**RegisterForm**
---

<Meta
    title="RegisterForm for SSO"
    component={RegisterForm}
/>


# RegisterForm for SSO
Sonr SSO form component for registration on the sonr network.

<ArgsTable of={RegisterForm} />

export const Template = (args) => <RegisterForm {...args} />

<Canvas>
    <Story name="Form"
      argTypes={{
        onRegister: {
          control: false
        },
        onError: {
          control: false
        }
      }}
      args={{
        domain: 'domain.snr',
        onRegister: result => alert(`Registered in! ${result}`),
        onError: err => alert(`Error: ${err}`)
    }}>
        {Template.bind({})}
    </Story>
</Canvas>
