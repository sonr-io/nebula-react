
<!-- LoginButton.stories.mdx -->

import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { LoginButton } from './../components/LoginButton';

---
name: Sonr SSO Login Button
route: /Components/**LoginButton**
---

<Meta
  title="Single Sign On Button"
  component={LoginButton}
  argTypes={{
    id: {control: false},
    onLogin: { control: false },
    onError : { control: false },
    Icon: { control: false },
  }}
/>

# Single Sign On Button
Sonr SSO button component for registration on the sonr network.

# Properties
```js
{
    type?: 'submit' | 'reset' | 'button' | undefined; //
    size?: ButtonSize;                      //
    skin?: ButtonSkin;                      //
    disabled?: boolean;                     //
    iconName?: IconNames;                   //
    iconType?: IconTypes;                   //
    domain: string;                         // snr domain naim for authentication
    label: string;                          // label for the button
    styling: string;                        // css style string
    onLogin: (result: boolean) => void;     // callback for post authentication steps
    onError: (error: any) => void;          //
}
```

export const Template = (args) => <LoginButton {...args} />

<Canvas>
    <Story name="Login"
      args={{
        label: "Login",
        domain: 'domain.snr',
        skin: 'primary',
        onLogin: result => alert(`Logged in! ${result}`),
        onError: err => alert(`Error: ${err}`)
    }}>
        {Template.bind({})}
    </Story>
</Canvas>
