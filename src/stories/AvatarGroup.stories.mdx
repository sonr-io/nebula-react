<!-- Avatar.stories.mdx -->

import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { AvatarGroup } from './../components/AvatarGroup';

---
name: Sonr Avatar Group
route: /Components/AvatarGroup
---

<Meta
  title="Avatar Group"
  argTypes={{ id: {control: false} }}
  component={AvatarGroup}
/>

# Avatar Group
Sonr SSO Avatar Group component.

# Properties
```js
{
    avatars: Array<Pick<AvatarProps, 'alt' | 'src'>>; // List of Avatar elements, but only using 'alt' or 'src' propperties
    size?: 'lg' | 'md' | 'sm';  // to define height and width for every Avatar, also limits the amount of avatar before rendeding the +N in the middle.
                                // sm being 4; md 5; and lg 6
}
```

export const Template = (args) => <AvatarGroup {...args} />

<Canvas>
  <Story name="Group" args={{ avatars: [{ alt: 'JD' }, { alt: 'JD' }, { alt: 'JD' }, { alt: 'JD' }], size: "lg" }}>
    {Template.bind({})}
  </Story>
</Canvas>
